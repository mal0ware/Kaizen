"""Identity & relationship graph (ADR 0003).

Entity (the real person/thing, may be absent) vs Account (an observed handle)
vs Belief (an observer's view of a subject). Objective facts attach to entities;
opinions/reputation attach to observer belief edges. Platform-agnostic.
"""
