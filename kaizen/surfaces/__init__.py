"""Surfaces: thin clients of the core (ADR 0004).

Planned: `discord/` (always-on gateway, own bot identity, shares Vixen infra)
and `terminal/` (Rich + prompt_toolkit TUI). The runnable dev CLI currently
lives in `kaizen.cli`; it will graduate into `terminal/` here. Not yet built.
"""
